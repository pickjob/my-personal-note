#!/bin/bash

# tmux 配置
# cp --force --verbose tmux.conf ~/.tmux.conf

# vim 相关配置
cp --force --verbose vimrc ~/.vimrc
cp --force --verbose base.vim ~/.vim
cp --force --verbose plugin.vim ~/.vim
cp --force --verbose myplugin.vim ~/.vim