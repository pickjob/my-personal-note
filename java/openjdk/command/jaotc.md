# jaotc命令学习
```bash
# 基本格式
jaotc [options] [name | list]
options:
    --output file
    --class-name class-names
    --jar jar-files
    --module modules
    --directory dirs
    --info
    --verbose
    --version
```